# Test Fixtures

Milestone 1 uses synthetic in-test CSI data so the canonical frame contract is
fully reproducible without external captures. Binary packet fixtures will be
added with the ESP32 parser milestone.

